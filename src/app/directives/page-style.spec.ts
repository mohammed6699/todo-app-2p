import { PageStyle } from './page-style';

describe('PageStyle', () => {
  it('should create an instance', () => {
    const directive = new PageStyle();
    expect(directive).toBeTruthy();
  });
});
