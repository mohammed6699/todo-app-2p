import { WordUpperPipe } from './word-upper-pipe';

describe('WordUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new WordUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
